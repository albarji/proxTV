language: python
matrix:
    include:
        - os: linux
          sudo: required
          python: "2.7"
          dist: trusty

        - os: linux
          sudo: required
          python: "3.4"
          dist: trusty

        - os: linux
          sudo: required
          python: "3.5"
          dist: trusty

        - os: linux
          sudo: required
          python: "3.6"
          dist: trusty

        - os: osx
          language: generic
          python: "2.7"

        - os: osx
          language: generic
          python: "3.4"

        - os: osx
          language: generic
          python: "3.5"

        - os: osx
          language: generic
          python: "3.6"

sudo: required
install:
    - pip install coveralls
script:
    # Build and install wheel
    - ./.travis/build.sh
    # Unit tests
    - nosetests --with-coverage --cover-package=prox_tv
after_success:
    - coveralls
